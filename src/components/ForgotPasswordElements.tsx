import styled from 'styled-components';
import { Form as FormPassForm } from './FormElements';

export const Form = styled(FormPassForm)`
  height: 50%;
`;

export * from '@/components/FormElements';
